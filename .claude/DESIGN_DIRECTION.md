# Design direction — locked

**Status: UNDECIDED.** No style has been chosen yet. This file is the single source of truth for the project's visual direction once it is. Every skill in `.claude/skills/` must read this before generating UI and follow it exactly — never re-pick a reference brand or switch generative skill per-component because of data density, screen type, or "it felt like a better fit here." Density differences are solved as *variants within the locked style* (tighter spacing, smaller type scale, denser grid — still the same design language), not by switching source.

Changing this file is a deliberate design decision, made once, with the user — not a side effect of building a particular screen.

---

## Once decided, fill in below and flip Status to LOCKED

**Reference brand(s)** (from `.claude/design-md/`): —

**Generative skill(s) in use for this project's dashboard views:** —

**Locked tokens** (copy the concrete values out of the chosen `DESIGN.md` so components stop re-reading the source file and can drift from each other by interpretation):
- Color palette (hex):
- Type scale / font stack:
- Spacing scale:
- Radius / border rules:
- Motion rules (if any):

**Decision log:**
- `YYYY-MM-DD` — chosen because ...
